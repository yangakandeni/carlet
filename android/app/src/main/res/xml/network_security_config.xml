<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- 
    DEVELOPMENT ONLY: Allow cleartext traffic for Firebase Emulators
    
    ⚠️ IMPORTANT FOR PRODUCTION:
    Remove or comment out this config before releasing to Play Store.
    Use build variants to automatically exclude this file from release builds:
    
    1. Create android/app/src/debug/res/xml/network_security_config.xml
    2. Delete this file from main/res/xml/
    3. Or use BuildConfig to conditionally apply only in debug builds
    -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
