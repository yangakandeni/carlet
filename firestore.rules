rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DEVELOPMENT ONLY: Allow all reads/writes. Replace before production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
